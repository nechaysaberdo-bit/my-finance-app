import { useRouter } from 'expo-router'; import { Screen, Card, Row, Money, Muted, Button } from '../components/ui'; import { cards } from '../data/mock';
export default function Credit(){const r=useRouter();return <Screen title="Credit cards ♡" kicker="Cards, installments & statements">{cards.map(c=><Card key={c.id} onPress={()=>r.push('/card')}><Row left={<><Money>{c.name}</Money><Muted>Cutoff every {c.cutoff}th</Muted></>} right={<Money>›</Money>}/><Row style={{marginTop:10}} left={<Muted>Current balance</Muted>} right={<Money>₱{c.balance.toLocaleString()}</Money>}/></Card>)}<Button title="+ Add credit card"/></Screen>}
